{
  "name": "alexa-jokes-lambda",
  "version": "1.0.0",
  "description": "A Lambda for the Alexa Jokes skill",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ask-sdk-core": "^2.0.0",
    "ask-sdk-model": "^1.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.15",
    "@types/node": "^10.12.15",
    "aws-lambda-typescript": "^3.0.0",
    "aws-sdk": "^2.377.0",
    "gulp": "^3.9.1",
    "ts-node": "^7.0.1"
  }
}
